# คู่มือเชิงปฏิบัติการ: สร้าง Dashboard โดยใช้ Looker Studio

## ภาพรวมโครงการเอกสาร
คู่มือนี้ถูกออกแบบให้เผยแพร่ได้ทั้ง HTML (Material for MkDocs) และไฟล์ PDF (MkDocs with PDF plugin) พร้อมตั้งค่า GitHub Actions สำหรับเผยแพร่อัตโนมัติบน GitHub Pages. โครงสร้างที่แนะนำสำหรับคลังข้อมูลคือ:

- `mkdocs.yml` — กำหนดธีม Material, ปลั๊กอินสร้าง PDF และเมนูนำทาง
- `docs/` — เนื้อหาคู่มือ (ไฟล์ Markdown)
- `.github/workflows/` — สคริปต์ GitHub Actions สำหรับ build และ deploy
- `requirements.txt` — รายการไลบรารี Python ที่ต้องใช้ในการ build

### วัตถุประสงค์ของ Workshop
ผู้เรียน (บุคลากรสายสนับสนุนและผู้ปฏิบัติงานทั่วไป) จะได้ฝึกปฏิบัติการตั้งแต่การเตรียมข้อมูล ไปจนถึงการสร้างรายงานและปรับจูนเพื่อใช้งานบนมือถือ โดยใช้ Looker Studio ร่วมกับข้อมูลตัวอย่างใน Google Sheets

> 📂 **ไฟล์ประกอบการอบรม:** ดาวน์โหลด [workshop_sample_data.csv](assets/workshop_sample_data.csv)
> แล้วอัปโหลดขึ้น Google Drive ของท่าน จากนั้น **ทำสำเนาไฟล์** ใน Google Sheets เพื่อใช้ฝึกปฏิบัติ

## ส่วนที่ 1: การเตรียมฐานข้อมูล (Data Preparation)

### 🚩 สถานการณ์จำลองที่ 1: "ข้อมูลดิบที่ยังไม่พร้อม"
- ได้รับ Google Sheets ที่ตั้งค่าเป็น "อ่านอย่างเดียว" และข้อมูลมีข้อความปนในตัวเลข
- เป้าหมายคือทำสำเนาและทำความสะอาดข้อมูลเพื่อพร้อมส่งเข้าระบบรายงาน

### Workshop 1: การจัดเตรียมและทำความสะอาดข้อมูล (Data Preparation & Cleaning)
**วัตถุประสงค์:** นำไฟล์ต้นฉบับมาเป็นของตนเองและตรวจสอบโครงสร้างข้อมูล (โฟกัสปีปัจจุบัน)

1. **การทำสำเนาไฟล์ (Make a Copy)**
   - เปิดลิงก์ไฟล์ประกอบการอบรม
   - ไปที่ **ไฟล์ (File) > ทำสำเนา (Make a copy)**
   - ตั้งชื่อไฟล์ใหม่ (เช่น `Workshop_ชื่อของท่าน`) แล้วกด **ทำสำเนา**
   - ✅ ผลลัพธ์: ได้ไฟล์ใหม่ที่แก้ไขข้อมูลได้เต็มรูปแบบ

2. **การจัดการหัวตาราง (Header)**
   - เลือกแผ่นงานปีล่าสุด (เช่น ปี 2567)
   - แถวที่ 1 ต้องเป็นชื่อคอลัมน์เท่านั้น (กิจกรรม, หน่วยปฏิบัติ, งบประมาณ)
   - ห้ามมีแถวว่างหรือชื่อรายงานอยู่เหนือหัวตาราง

3. **การทำความสะอาดข้อมูลตัวเลข**
   - ตรวจสอบคอลัมน์ **งบประมาณ** และ **ยอดเบิกจ่าย** ให้เป็นตัวเลขล้วน
   - ตัวอย่าง: `50,000 บาท` → ลบคำว่า "บาท" เหลือ `50000`
   - เคล็ดลับ: ถ้าเลขชิดขวาแสดงว่าเป็นตัวเลข ถ้าชิดซ้ายคือข้อความ

4. **การเพิ่มคอลัมน์คำนวณ (Calculated Column)**
   - สร้างคอลัมน์ใหม่ด้านขวาสุด ตั้งชื่อว่า **งบประมาณคงเหลือ**
   - ใส่สูตร `=(งบประมาณ) - (ยอดเบิกจ่าย)` แล้วลากสูตรลงทุกแถว

5. **การตรวจสอบความครบถ้วน**
   - ตรวจสอบคอลัมน์ **แหล่งงบประมาณ** ต้องระบุว่า "ส่วนกลาง" หรือ "ส่วนภูมิภาค" ครบทุกแถว

## ส่วนที่ 2: การสร้างรายงานด้วย Looker Studio

### 🚩 สถานการณ์จำลองที่ 2: "เปลี่ยนตัวเลขให้เป็นภาพ"
- ผู้บริหารต้องการเห็นภาพรวมการเบิกจ่ายปี 2567 แบบรวดเร็ว ไม่ต้องอ่านตารางยาว

### Workshop 2: การเชื่อมต่อข้อมูล (Data Connection)
1. เข้า Looker Studio > **Create > Report**
2. เลือก Connector เป็น **Google Sheets**
3. เลือกไฟล์ที่ทำสำเนาไว้ (Workshop 1) และแผ่นงานปีปัจจุบัน (เช่น "ปี 67")
4. กด **Add** เพื่อนำข้อมูลเข้าสู่รายงาน

### Workshop 3: การสร้างการแสดงผล (Visualization)
1. **ชื่อรายงาน** — ใช้ Text ใส่หัวข้อใหญ่ด้านบน: `รายงานติดตามงบประมาณ ประจำปี 2567`
2. **สัดส่วนแหล่งงบประมาณ (Pie Chart)**
   - Dimension: แหล่งงบประมาณ
   - Metric: งบประมาณ
   - คำถาม: งบปีนี้มาจากส่วนกลางหรือภูมิภาคมากกว่ากัน?
3. **สถานะการเบิกจ่ายภาพรวม (Gauge/Scorecard)**
   - สร้าง Field ใหม่ชื่อ **% การเบิกจ่าย** ด้วยสูตร `SUM(ยอดเบิกจ่าย) / SUM(งบประมาณ)`
   - ใช้เป็น Metric และตั้ง Type เป็น Percent (%)
   - คำถาม: เบิกจ่ายไปแล้วกี่เปอร์เซ็นต์?
4. **การเบิกจ่ายจำแนกตามกลุ่ม (Stacked Bar / Column)**
   - กราฟ 4.1 (แหล่งงบ): Dimension = แหล่งงบประมาณ, Metric = ยอดเบิกจ่าย + งบประมาณคงเหลือ
   - กราฟ 4.2 (ชื่อกิจกรรม): Dimension = ชื่อกิจกรรม, Metric = ยอดเบิกจ่าย + งบประมาณคงเหลือ
   - กราฟ 4.3 (หน่วยปฏิบัติ - แนวตั้ง): Dimension = หน่วยปฏิบัติ, Metric = ยอดเบิกจ่าย + งบประมาณคงเหลือ
5. **ตารางสรุปรายละเอียด (Pivot Table)**
   - Row Dimension: หน่วยปฏิบัติ
   - Column Dimension: ชื่อกิจกรรม
   - Metrics: งบประมาณ, ยอดเบิกจ่าย, งบประมาณคงเหลือ (GF)

## ส่วนที่ 3: การยกระดับสู่ระบบฐานข้อมูลรวม (Advanced Consolidation)

### 🚩 สถานการณ์จำลองที่ 3: "เมื่อปีงบประมาณเปลี่ยน"
- หัวหน้าต้องการดูข้อมูลปี 65, 66, 67 และเตรียมพร้อมปี 68 ในกราฟเดียว
- แนวทาง: รวมข้อมูลทุกปีเป็น **Master Data** แผ่นเดียว

### Workshop 4: การรวมศูนย์ข้อมูล (Creating Master Data)
1. สร้างแผ่นงานใหม่ชื่อ **Master_Data**
2. โครงสร้าง: คอลัมน์ A = **ปีงบประมาณ**, คอลัมน์ถัดไปคือหัวตารางเดิมจากแต่ละปี
3. รวมข้อมูล:
   - คัดลอกจากแผ่นงานปี 65 แล้วใส่ปี `2565` ในคอลัมน์ปีงบประมาณทุกแถว
   - ทำซ้ำกับปี 66 (`2566`) และปี 67 (`2567`)
4. ✅ ผลลัพธ์: มีตารางยาวรวมทุกปีในแผ่นงานเดียว

### Workshop 5: การอัปเกรดรายงานและตัวกรอง (Reconnect & Filters)
1. ใน Looker Studio ไปที่ **Resource > Manage added data sources**
2. กด **Edit** แหล่งข้อมูลเดิมแล้วเปลี่ยนแผ่นงานเป็น **Master_Data** จากนั้น **Reconnect**
3. เพิ่มตัวเลือกปี: **Add a control > Drop-down list** และตั้งค่า field = ปีงบประมาณ
4. ทดสอบ: โหมด View แล้วเปลี่ยนปีใน Dropdown กราฟทั้งหมดจะอัปเดตตาม

## ส่วนเสริม: ปรับใช้งานบนมือถือ (Mobile Responsive)

### 🚩 สถานการณ์จำลองที่ 4: "รายงานฉบับพกพา"
- ผู้บังคับบัญชาชมผ่านมือถือบ่อย ต้องการตัวหนังสือใหญ่และกดง่าย

### Workshop 6: การตั้งค่าการแสดงผล (Layout Settings)
1. คลิกพื้นที่ว่างของรายงาน > **Theme and layout**
2. แท็บ **Layout** > เลือก **Display Mode** เป็น **Fit to width**
3. ✅ ผลลัพธ์: รายงานขยายเต็มความกว้างจอมือถือ อ่านง่ายไม่ต้องซูม

**Pro Tip:** จัดกราฟเรียงในแนวตั้งเพื่อให้เลื่อนดูสะดวกบนมือถือ

## บทสรุปและแนวทางเผยแพร่เอกสาร
- ผู้เรียนได้ฝึกตั้งแต่การเตรียมข้อมูลปีเดียว → รวมหลายปี → ปรับแต่งสำหรับมือถือ
- การเตรียมข้อมูล (Data Prep) คือหัวใจสำคัญ: ถ้าจัดเป็นรูปแบบ **Master Data** จะสร้างรายงานครั้งเดียวและใช้ต่อได้ยาวนาน

### ขั้นตอน Build & Deploy ด้วย Material for MkDocs
1. ติดตั้งเครื่องมือ:
   ```bash
   pip install -r requirements.txt
   ```
2. สร้างเอกสาร HTML และ PDF:
   ```bash
   mkdocs build --strict
   ```
   - HTML จะอยู่ในโฟลเดอร์ `site/`
   - PDF จะถูกสร้างที่ `site/pdf/looker-studio-workshop.pdf`
3. พรีวิวเอกสารแบบโลคัล:
   ```bash
   mkdocs serve
   ```
   แล้วเปิด `http://127.0.0.1:8000`

### GitHub Actions สำหรับ GitHub Pages
เวิร์กโฟลว์ใน `.github/workflows/deploy.yml` จะรันเมื่อมีการ push ไปยังสาขา `main` หรือสั่ง `workflow_dispatch` โดยอัตโนมัติจะ:

- ติดตั้ง dependencies ที่จำเป็นสำหรับ Material for MkDocs และปลั๊กอิน PDF
- รัน `mkdocs build --strict` เพื่อตรวจสอบเนื้อหา
- รัน `mkdocs gh-deploy --force --remote-branch gh-pages` เพื่อเผยแพร่ไปยัง GitHub Pages โดยใช้ `GITHUB_TOKEN`

> หลังการดีพลอย ให้ตั้งค่าหน้า GitHub Pages ของคลังให้ใช้สาขา `gh-pages` และโฟลเดอร์รากเป็นแหล่งเผยแพร่
